# Vercel Marketplace の Upstash Redis 接続時に自動設定されます。
# 新しい統合では UPSTASH_*、既存のVercel KV互換統合では KV_* のどちらか一組を使えます。
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# 既存のVercel KV互換名を使う場合はこちら（上の空欄はそのままでも動作します）。
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
